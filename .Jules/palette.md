## 2026-02-14 - Back to Top UX Pattern
**Learning:** Fixed threshold (1vh) is better than percentage for "Back to Top" visibility. Removing conditional rendering allows CSS transitions for delightful UX.
**Action:** Use `window.innerHeight` and CSS transitions + `aria-hidden` for interactive floating buttons.
