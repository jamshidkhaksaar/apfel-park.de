import AdminShell from "@/components/admin/AdminShell";import AdminCampaignManager from "@/components/admin/AdminCampaignManager";import { getAdminLocale } from "@/lib/admin-i18n-server";export default async function CampaignsPage(){const locale=await getAdminLocale();return <AdminShell title={locale==="de"?"Kampagnen & Gutscheine":"Campaigns & coupons"}><div className="mx-auto w-full max-w-[1500px]"><p className="mb-5 text-sm text-muted">{locale==="de"?"Serverseitige Regeln verwalten. Aktivieren Sie Codes erst nach der Zahlungsintegration.":"Manage server-side rules. Enable codes only after payment integration."}</p><AdminCampaignManager locale={locale}/></div></AdminShell>}
